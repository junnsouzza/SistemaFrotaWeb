@model IEnumerable<FrotaSisWeb.Models.SisModels.SisUf>

@{
    ViewData["Title"] = "Index";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h2>Index</h2>

            <p>
                <a asp-action="Create">Create New</a>
            </p>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.DescUf)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.DescUf)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.IdUf">Edit</a> |
                                <a asp-action="Details" asp-route-id="@item.IdUf">Details</a> |
                                <a asp-action="Delete" asp-route-id="@item.IdUf">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>